#
# ~/.bashrc
#

# Show system info on terminal start
if [ -t 1 ]; then
    neofetch
fi

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='eza -lao --git --no-user --color=always --icons=always'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '
